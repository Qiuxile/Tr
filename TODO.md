# Tr 开发路线 (v2.0.0+ CLI/TUI)

> 项目已精简为纯终端形态:Go CLI + bubbletea TUI,无 GUI/Web 依赖。

## 已完成 (v2.0.0)
- [x] 移除 Wails 桌面壳、Vue 前端与浏览器 GUI,依赖收敛到 bubbletea 一族
- [x] CLI 兼容:v1 的翻译/管道/-config/-o/-help 等用法全部保留
- [x] 新增 TUI:多行输入、结果滚动、Ctrl+S 互换方向、Ctrl+O 离线切换
- [x] 中/英/日 界面文案随 TUI 迁移更新

## 待办想法
- [x] CLI `->` 箭头语法:自动识别源语言并翻译到指定语言(2026, v2.x)
- [x] AI 翻译后端:OpenAI 兼容接口 + `api_key` 配置 + 省 token 策略(缓存优先、关闭思考、限制输出)
- [x] 文件翻译:`tr -f <文件> [输出]`,分块翻译、行结构保持、写入文件或目录
- [x] 移除免费 API / 自建 API 后端,翻译只走 AI(`-o` 时用缓存 + 内置词典离线降级)
- [x] `->` 目标语言与 TUI `Ctrl+S` 互换只作用于本次执行,不再写配置文件
- [x] 帮助文档列出可用目标语言简写(中/英/日三语)
- [x] 翻译函数接收显式 target 参数(空=读配置;有箭头=只用箭头值)
- [x] TUI 改左右布局,原文/译文标题中的语言可聚焦(Tab 循环 + ←/→ 切换)
- [x] 语言切换改为 Alt+方向键直切(Alt+←/→ 目标语言,Alt+↑/↓ 源语言),不再占用 Tab 焦点
- [x] 按键定稿:翻译 `Ctrl+T`,语言切换 `Ctrl+←/→`(按当前焦点切源/目标),`Enter` 换行
- [x] `Ctrl+L` 改为清空终端输出(翻译界面清译文区、系统命令行清屏),输入框清空移到 `Ctrl+U`
- [x] TUI `Esc` 进入命令行模式,`Ctrl+L` 清空输入
- [x] 命令行模式改为真正的系统命令行(隐藏翻译面板、整屏显示命令输出、↑/↓ 历史),移除自定义命令集
- [x] TUI 逻辑回归测试(布局/焦点/命令解析/不写配置)
- [ ] 文件翻译并行化(多块并发送,受速率限制约束)
- [ ] TUI 显示 token 用量(读取响应 usage 字段)
- [ ] 拉丁语系细分:英文/法文/德文等基于拉丁字母文本的自动识别
- [ ] TUI 增加会话历史浏览(↑/↓ 翻阅已翻译条目)
- [ ] 词典扩充:英→日、中→英 词条
- [ ] `tr config set` 增加语言代码合法性校验
- [ ] Linux / macOS 下的 CI 构建与发布
- [ ] TUI 支持鼠标滚轮滚动结果区
