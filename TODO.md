# Tr 重构开发计划 (Roadmap)

> **目标**：从终端翻译工具完全重构为面向大众的桌面划词/截屏翻译助手。  
> **技术栈**：Go (后端) + Wails (桌面框架) + Vue/React (前端弹窗)。

---

## 🚩 第一阶段：基建与骨架 (P0 - 核心基础)
- [ ] **1.1 环境初始化**
  - [ ] 安装 Wails CLI，执行 `wails init` 生成项目骨架。
  - [ ] 配置 `go.mod`，整理依赖管理。
- [ ] **1.2 弹窗界面设计**
  - [ ] 配置窗口为 **无边框 (`Frameless`)**、**透明背景**、**置顶 (`AlwaysOnTop`)**。
  - [ ] 设定初始窗口尺寸 (建议 450x350)，外观采用毛玻璃效果 (CSS Glassmorphism)。
  - [ ] 实现窗口失焦自动隐藏 (Blur 事件)。
- [ ] **1.3 系统托盘 (System Tray)**
  - [ ] 编写常驻托盘图标，支持“显示主界面”、“退出”功能。
  - [ ] 托盘菜单支持“开机自启”切换开关 (预留设置入口)。

## 🚩 第二阶段：翻译核心引擎 (P0 - 核心逻辑)
- [ ] **2.1 后端 API 聚合**
  - [ ] 封装多翻译源接口 (MyMemory, LibreTranslate, 百度翻译 API 等)。
  - [ ] 实现统一调用接口 `Translate(text, targetLang)`。
- [ ] **2.2 本地缓存机制**
  - [ ] 实现 `cache.json` 读写，采用 LRU (最近最少使用) 淘汰策略。
  - [ ] 重复查询命中缓存，直接返回，避免重复 HTTP 请求。
- [ ] **2.3 输入交互联动**
  - [ ] 前端输入框监听 `Ctrl+Enter` 触发展开翻译。
  - [ ] 前端实时接收 Go 后端返回的结果并渲染 (原文/译文双栏布局)。

## 🚩 第三阶段：全局划词翻译 (P0 - 核心痛點)
- [ ] **3.1 全局快捷键**
  - [ ] 使用 `hook` 或 `robotgo` 监听 `Ctrl+Shift+T` (默认) 唤出/隐藏窗口。
- [ ] **3.2 划词取词逻辑 (难点)**
  - [ ] 监听鼠标松开事件，判定是否有文字选中。
  - [ ] 模拟系统复制 (`Ctrl+C` / `Command+C`)。
  - [ ] 读取剪贴板内容，过滤空白/重复内容。
  - [ ] **权限引导**：Mac 检测辅助功能权限，Windows 检测管理员权限，未授权时引导用户开启。
- [ ] **3.3 智能弹窗定位**
  - [ ] 获取鼠标当前屏幕坐标，将弹窗显示在鼠标指针附近 (防止遮挡视线)。

## 🚩 第四阶段：截屏与 OCR 翻译 (P1 - 增强功能)
- [ ] **4.1 触发截图模式**
  - [ ] 绑定快捷键 `Ctrl+Shift+S` 进入截图选区模式。
  - [ ] 前端/后端调用 `screenshot` 库截取全屏作为画布。
- [ ] **4.2 区域框选与识别**
  - [ ] 实现鼠标拖拽框选区域 (截取子图)。
  - [ ] **OCR 对接**：封装百度/有道 OCR API (优先) 或 Tesseract 离线引擎。
- [ ] **4.3 自动翻译与回显**
  - [ ] OCR 识别出的文字自动送入翻译引擎，在弹窗中显示。

## 🚩 第五阶段：用户体验与设置界面 (P1 - 完善迭代)
- [ ] **5.1 设置页面 (Settings)**
  - [ ] 允许用户切换默认翻译源 (Google/有道/DeepL 等)。
  - [ ] 允许用户自定义全局快捷键 (Key Mapping)。
  - [ ] 允许用户开启/关闭“划词自动弹窗”和“开机自启”。
- [ ] **5.2 交互优化**
  - [ ] 无操作 5 秒后自动淡出/关闭弹窗。
  - [ ] 增加复制按钮，一键复制译文到剪贴板。
  - [ ] 增加“朗读”按钮 (TTS 语音合成)。

## 🚩 第六阶段：打包与分发 (P2 - 交付上线)
- [ ] **6.1 跨平台编译**
  - [ ] 使用 `wails build` 生成 Windows (.exe) 和 macOS (.app) 二进制文件。
- [ ] **6.2 安装包制作**
  - [ ] Windows: 使用 Inno Setup 生成安装向导。
  - [ ] macOS: 生成 `.dmg` 镜像文件。
- [ ] **6.3 签名与公证**
  - [ ] (非必须但建议) 申请代码签名证书，减少杀毒软件误报和系统拦截。

---

## 📋 开发优先级说明
- **P0 (必须完成)**：阶段 1-3，做完即可发布 MVP 公测版。
- **P1 (体验增强)**：阶段 4-5，提升工具差异性。
- **P2 (锦上添花)**：阶段 6，正式分发前的最后工序。